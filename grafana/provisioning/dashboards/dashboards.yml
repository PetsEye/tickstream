apiVersion: 1

providers:
  - name: tickstream
    orgId: 1
    folder: tickstream
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
